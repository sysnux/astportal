#
# astportal2 - Pylons development environment configuration
#
# The %(here)s variable will be replaced with the parent directory of this file
#
# This file is for deployment specific config options -- other configuration
# that is always required for the app is done in the config directory, 
# and generally should not be modified by end users. 

[DEFAULT]
debug = true
# Uncomment and replace with the address which should receive any error reports
#email_to = you@yourdomain.com
smtp_server = localhost
error_email_from = paste@localhost

[server:main]
use = egg:Paste#http
host = 0.0.0.0
port = 8080

[app:main]
use = egg:astportal2
full_stack = true
#lang = ru
cache_dir = %(here)s/data
beaker.session.key = astportal2
beaker.session.secret = c5f3e81dd8f0608e933b23622cfedbab216b6ebb695b61ca4eeb5f9599e95470

# If you'd like to fine-tune the individual locations of the cache data dirs
# for the Cache data, or the Session saves, un-comment the desired settings
# here:
#beaker.cache.data_dir = %(here)s/data/cache
#beaker.session.data_dir = %(here)s/data/sessions

# pick the form for your database
# %(here) may include a ':' character on Windows environments; this can
# invalidate the URI when specifying a SQLite db via path name
sqlalchemy.url=postgresql://postgres@:5432/astportal2
sqlalchemy.use_native_unicode = False

#echo shouldn't be used together with the logging module.
sqlalchemy.echo = False
sqlalchemy.echo_pool = False
sqlalchemy.pool_recycle = 3600

# if you are using Mako and want to be able to reload
# the mako template from disk during the development phase
# you should say 'true' here
# This option is only used for mako templating engine
# WARNING: if you want to deploy your application using a zipped egg
# (ie: if your application's setup.py defines zip-safe=True, then you
# MUST put "false" for the production environment because there will
# be no disk and real files to compare time with.
# On the contrary if your application defines zip-safe=False and is
# deployed in an unzipped manner, then you can leave this option to true
templating.mako.reloadfromdisk = true

# the compiled template dir is a directory that must be readable by your
# webserver. It will be used to store the resulting templates once compiled
# by the TemplateLookup system.
# During development you generally don't need this option since paste's HTTP
# server will have access to you development directories, but in production
# you'll most certainly want to have apache or nginx to write in a directory
# that does not contain any source code in any form for obvious security reasons.
#
#templating.mako.compiled_templates_dir = /some/dir/where/webserver/has/access

templating.genshi.method = html

# WARNING: *THE LINE BELOW MUST BE UNCOMMENTED ON A PRODUCTION ENVIRONMENT*
# Debug mode will enable the interactive debugging tool, allowing ANYONE to
# execute malicious code after an exception is raised.
#set debug = false

# Asterisk manager connections
asterisk.manager = (['asterisk.sysnux.pf', 'astman', 'astmaster'],) # ['tiare.sysnux.pf', 'cel', 'cel'])

server.sip = asterisk.sysnux.pf
server.firmware = tiare.sysnux.pf
server.config = tiare.sysnux.pf
server.syslog = tiare.sysnux.pf
server.ntp = tiare.sysnux.pf

# Q30
# asterisk.manager = (['localhost', 'astman', 'astmaster'],) # ['tiare.sysnux.pf', 'cel', 'cel'])
company = 'SysNux'
default_dnis = '501040'
# server.sip = 192.168.199.1
# server.firmware = 192.168.199.1
# server.config = 192.168.199.1
# server.syslog = 192.168.199.1
# server.ntp = 192.168.199.1

# Mandriva
command.fping = /bin/fping -n -q
command.arp = /sbin/arp -n
command.sox = /usr/bin/sox "%s" -r 8000 -c 1 -b 16 %s
# CentOS
# command.fping = /usr/sbin/fping -n -q
# command.sox = '/usr/bin/sox "%s" -r 8000 -c 1 -w %s

directory.tftp = /var/lib/tftpboot/
directory.asterisk = /etc/asterisk/astportal/

#----------------------------------
# Logging configuration
# Add additional loggers, handlers, formatters here
# Uses python's logging config file format
# http://docs.python.org/lib/logging-config-fileformat.html

[loggers]
keys = root, astportal2, sqlalchemy, auth

[handlers]
keys = console

[formatters]
keys = generic

# If you create additional loggers, add them as a key to [loggers]
[logger_root]
level = INFO
handlers = console

[logger_astportal2]
level = DEBUG
handlers =
qualname = astportal2

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine
# "level = INFO" logs SQL queries.
# "level = DEBUG" logs SQL queries and results.
# "level = WARN" logs neither.  (Recommended for production systems.)


# A logger for authentication, identification and authorization -- this is
# repoze.who and repoze.what:
[logger_auth]
level = WARN
handlers = 
qualname = auth

# If you create additional handlers, add them as a key to [handlers]
[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

# If you create additional formatters, add them as a key to [formatters]
[formatter_generic]
format = %(asctime)s,%(msecs)03d %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S

